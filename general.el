; -*- mode: elisp -*-
(setq user-full-name "Om Prakash Singh"
      user-mail-address "ompnix@gmail.com")

;; emacs initialization
(setq inhibit-splash-screen t) ;; removes the emacs splash screen and loads scratch buffer
